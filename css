.square-header {
  width: 425px;         /* Define a width */
  aspect-ratio: 1 / 1; /* Height is 25% of the width to maintain square aspect ratio */
  background-color: transparent;
  position: relative; /* Needed for positioning content inside the header */
  border-right: black 4px solid;
  margin-top: -10px;
}
/* Style the content inside to be centered vertically and horizontally */
.square-header h1 {
  width: 80%;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-75%, -10%);
  top: 1px;
  padding-left: 10px;
}
img {
  max-width: 150%; /* Image will scale down to fit its container, but never exceed 100% of its original width */
  height: auto;    /* Maintains the aspect ratio */
  display: block;
  margin: 0 auto; /* Center the image horizontally */
  position: center;
}

/* Utility: pin a specific heading to the top without affecting headings inside .square-header */
.fixed-top {
  position: fixed;
  top: 100px;
  left: 0;
  right: 0;
  width: 100%;
  z-index: 9999;
  background: transparent;
  text-align: center;
  padding: 12px 0;
}
/* Utility: make an image visually smaller (use by adding class="img-small") */
.img-small {
  max-width:100px;
  width: 50%;
  height: auto;
  display: inline-block;
}
h2 {
  font-size: 2rem;
  color: #555;
  margin: 0;
  text-align: left;
  padding-top: 100px 0;
}
.bg-card-image {
  display: flex;
  align-items: left;
  height: 225px;
  justify-content: left;
}

/* Vertically center Abilities content inside the left column */
.abilities-column {
  display: flex;
  flex-direction: column;
  align-items: left;
  align-items: left;
  justify-content: left;
  height: 425px; /* match `.square-header` size for vertical alignment */
  width: 100px; /* ensure padding/border don't affect size */
  border: 4px solid black; /* match .square-header right-border style */
  border-top: 2px solid black;
  border-left: none;
  border-bottom: 2px solid black; /* join seamlessly with header above */
  margin-top: -4px; /* overlap so borders appear continuous */
  margin-right: 4px; /* overlap so borders appear continuous */
  padding-top: 18px;
  padding-bottom: 20px;
}
.abilities-column .text {
  position: relative;
  right: -120px;
  margin-bottom: 8px;
}
.abilities-column .bg-card-image {
  height: auto;
  position: relative;
}
.abilities-column .bg-card-image img {
  max-height: 420px;
  width: auto;
  height: auto;
  display: block;
}
.talker-image {
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  height: 300px;
}
.talker-image img {
  width: 650px;
  max-width: none;
  height: auto;
  display: block;
  margin: 0 auto;
  position: relative;
  top: -150px;       /* moved further up */
  left: -200px;       /* shifted to the left */
  z-index: 1000;
  border: 2px solid #000000;
  margin-right: 100px;
  margin-top: 125px;
}

.card-image {
  top: -400px;
  right: -105px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  border: 4px solid black; /* match .square-header right-border style */
}

.caller-image {
  display: inline-block;
  padding-top: 0px; /* space between image and border */
  border: 2px solid #000000;
  background: transparent;
  margin: 12px auto; /* center horizontally when possible */
  position: relative;
  z-index: 1000;
  top: -185px;
  left: 200px;
}
.caller-image img {
  display: block;
  width: auto;
  max-width: 200px; /* adjust size as needed */
  height: auto;
}
.contact-me {
  border-bottom: 2px solid #000000;
  padding-top: 30px;
  padding-bottom: 25px;
  font-weight: bold;
  font-size: 25px;
  color: #494949;
  text-align: center;
}
